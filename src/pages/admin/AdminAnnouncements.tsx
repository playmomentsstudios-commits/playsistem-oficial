import { useEffect,useState } from 'react'
import { portalApi } from '../../api/portal'
import { useAuth } from '../../contexts/AuthContext'
import { useToast } from '../../contexts/ToastContext'
export function AdminAnnouncements(){const {user}=useAuth(),toast=useToast();const [rows,setRows]=useState<any[]>([]),[title,setTitle]=useState(''),[content,setContent]=useState('');const load=()=>portalApi.announcements().then(setRows);useEffect(()=>{void load()},[]);async function create(){if(!title||!content)return;try{await portalApi.createAnnouncement({title,content,audience:'customers',active:true,created_by:user?.id});setTitle('');setContent('');toast('Comunicado publicado.','success');await load()}catch(e:any){toast(e.message,'error')}}return <div><h1 className="text-2xl font-bold text-white mb-2">Comunicados</h1><p className="text-sm text-gray-500 mb-6">Publique atualizações para clientes</p><div className="p-4 rounded-2xl bg-[#141416] border border-white/10 mb-5"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Título" className="w-full px-3 py-2 rounded bg-white/5 border border-white/10"/><textarea value={content} onChange={e=>setContent(e.target.value)} placeholder="Mensagem" className="mt-2 w-full px-3 py-2 rounded bg-white/5 border border-white/10"/><button onClick={create} className="mt-2 px-4 py-2 rounded bg-[#E30613]">Publicar</button></div><div className="space-y-2">{rows.map(a=><div key={a.id} className="p-4 rounded-2xl bg-[#141416] border border-white/10"><b>{a.title}</b><p className="text-sm text-gray-400 mt-1">{a.content}</p></div>)}</div></div>}
