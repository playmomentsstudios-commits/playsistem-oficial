# Public frontend API base URL. Leave empty until the Supabase backend is standardized.
# When empty, the current client uses /api; no API proxy is configured on Netlify.
# VITE_ variables are included in the browser bundle. Never put private keys here.
VITE_API_URL=
